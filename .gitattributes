# Modern standard: LF everywhere (align with .NET Core+, Docker, cross-platform)
* text=auto eol=lf

# Windows-specific files that MUST use CRLF (will break otherwise)
*.bat text eol=crlf
*.cmd text eol=crlf

# Denote all files that are truly binary and should not be modified
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.dll binary
*.exe binary
*.pdf binary
*.zip binary
